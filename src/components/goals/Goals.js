import React from 'react';
import Goal from './Goal';

const Goals = (props) =>
    <ul>
        {/* { props.goals.map((goal, id) => <Goal key={id} goal={goal}/>) } */}
    </ul>
        
export default Goals